# 2026-06-01

## 项目改名为 remoteid-monitor

修改范围：
- `backend/go.mod`: module `remoteid` → `remoteid-monitor`
- 所有 Go 文件的 import 路径：`"remoteid/..."` → `"remoteid-monitor/..."`
- `backend/build.sh` / `backend/deploy.sh`：二进制名、部署路径、systemd 服务名均更新
- `deploy/Dockerfile.backend`：二进制名、容器名更新
- `deploy/docker-compose.yml`：容器名、volume 名更新
- `backend/internal/config/config.go`：默认 db/log 路径更新
- `backend/internal/config/env.go`：环境变量前缀 `REMOTEID_` → `REMOTEID_MONITOR_`
- `backend/cmd/ridparse/main.go`：标题文字更新
- `frontend/package.json`：deploy 脚本路径更新
- `README.md`：标题和示例路径更新
- `.gitignore`：build artifacts 路径更新
- `MEMORY.md`：项目名称和部署路径更新

编译验证通过，无 lint 错误。

## 修复操作者 ID 被当作 UAS ID 的问题

参考 `/Users/luolitao/remoteid/RemoteID/open-remote-id-parser-master/include` 中的类型定义，将 Operator ID 与 UAS ID 分离存储和显示。

修改文件：

**backend/pkg/types/models.go**：
- `DroneData` 新增 `OperatorID string` 字段（json: `operator_id`）

**backend/internal/drone/processor.go**：
- `updateDroneFromMessage` 新增 `"operator_id"` case，将消息中的 `operator_id` 存入 `drone.OperatorID`

**backend/internal/db/sqlite.go**：
- `drones` 表新增 `operator_id TEXT` 列（建表、迁移、查询 SELECT、INSERT、UPSERT）
- `GetActiveDrones` 查询和 Scan 中加入 `operator_id` 字段
- `SaveDrone` INSERT 和 UPDATE 中加入 `operator_id`

**frontend/src/App.vue**：
- 侧边栏详情面板中 Type/Standard 区域新增 "Op. ID" 显示行
- Operator 区域新增 `operator_id` 显示（条件渲染 `v-if="selectedDrone.operator_id || selectedDrone.operator_latitude"`）

**frontend/src/views/DroneDetail.vue**：
- Operator 区域新增 "Op. ID" 显示行
- 面板可见性条件扩展为 `drone?.operator_latitude || drone?.operator_id`

**frontend/src/views/Drones.vue**：
- 表格新增 "Op. ID" 列（位于 ID 和 MAC 之间）
- colspan 从 8 更新为 9

编译验证通过。

## 前端布局调整：按钮组和详情面板移至右侧

修改 `frontend/src/App.vue`：
- `#header_side`（地图类型切换按钮组 + 设置齿轮）：`left: 6px` → `right: 6px`，从左上角移到右上角
- `#selected_infoblock`（选中无人机详情面板）：`left: 6px` → `right: 6px`，从左下角移到右下角

## 地图初始缩放级别最大化 + 侧边栏保持显示

- 地图 `setView` zoom 从 10 → 18（最大级别）
- `sidebarOpen` 保持 `true`（默认显示侧边栏）

## 无人机详情合并至侧边栏，去掉过滤列表

修改 `frontend/src/App.vue`：
- 移除地图上的 `#selected_infoblock` 详情浮块
- 移除侧边栏内的 `#sidebar_controls`（过滤/列选择面板）
- 无人机详情信息块移至侧边栏内表格下方（`#sidebar_drone_detail`），选中无人机后展开
- 删除 `showCompliant`/`showNonCompliant`/`showStale`/`showColumnPanel`/`toggleColumn` 等不再使用的变量和函数
- `filteredDrones` 简化为仅保留搜索过滤
- 清理相关 CSS（`.filter-row`/`.filter-chip`/`.column-chip`/`.sr-only`/`.columns-grid` 等）

## 移除 GB42590-2023 国标协议解析，聚焦 ASTM/ASD-STAN

因对国标协议理解有误影响 RID 解析效果，暂时去掉与国标相关的所有解析功能，以 ASTM 和 ASD 为主进行 WiFi 信号抓取和 RID 数据解析，涵盖 WiFi Beacon 和 NAN。

修改文件：

**backend/internal/drone/parser.go** (1071 → 528 行)：
- 删除协议概述中的 GB42590/GB46750 描述
- 删除 `gb42590InterfaceVersion`、`gb42590PackedHeader` 常量
- 简化 `parseVendorIE()`：移除 GB42590/ASTM 的区分逻辑，统一按 ASTM 解析
- 简化 `parseNANAttributes()`：移除 GB42590 格式判断分支，统一按 ASTM 解析
- 删除全部 GB 解析函数：`parseGB42590BeaconMessages`、`decodeGB42590Message`、`decodeGB42590Location`、`decodeGB42590System`、`parseGB42590Packed`
- 删除全部 GB 辅助函数：`getGBUAtypeName`、`getGBIDTypeName`、`getGBStatusName`、`getGBDescTypeName`、`getGBCoordSysName`、`getGBClassRegionName`、`getGBStationLocTypeName`、`getGBUACategoryName`、`getGBUAClassName`、`getGBHorizontalAccuracy`、`getGBVerticalAccuracy`、`getGBSpeedAccuracy`

**backend/internal/drone/processor.go**：
- 移除 `updateDroneFromMessage` 中的 `ChinaCompliant` 赋值逻辑

**backend/pkg/types/models.go**：
- 移除 `DroneData.ChinaCompliant` 字段

**backend/internal/db/sqlite.go**：
- 移除 `drones` 表中的 `china_compliant` 列（建表、查询、插入、更新）
- 删除 `GetCompliantDroneCount` 函数
- 删除 `boolToSQLiteBool` 辅助函数

**frontend/src/App.vue**：
- 移除 `china_compliant` 列定义
- 移除表格中 `china_compliant` 列的渲染模板
- 移除详情面板中的 Compliance 行
- `getRowClass()` 简化颜色逻辑
- `getDroneColor()` 按 standard 字段判断颜色

**frontend/src/views/Drones.vue**：
- 移除 `china_compliant` 合规状态标签列

**frontend/src/views/DroneDetail.vue**：
- 用 standard 名称替代 `china_compliant` 合规状态展示
- 地图标记颜色改为固定蓝色

**frontend/src/components/DroneMap.vue**：
- `china_compliant` 颜色逻辑改为按 `standard` 判断

**README.md**：
- 移除 GB42590-2023 协议说明章节
- 更新协议概述和功能描述

编译验证通过，无 lint 错误。

## 修复操作者 ID 被当作 UAS ID 的问题

参考 `/Users/luolitao/remoteid/RemoteID/open-remote-id-parser-master/include` 中的类型定义（`UAVObject.id` vs `UAVObject.operator_id.id`），将 Operator ID 与 UAS ID 分离存储和显示。

修改文件：

**backend/pkg/types/models.go**：
- `DroneData` 新增 `OperatorID string` 字段（json: `operator_id`）

**backend/internal/drone/processor.go**：
- `updateDroneFromMessage` 新增 `"operator_id"` case，将消息中的 `operator_id` 存入 `drone.OperatorID`

**backend/internal/db/sqlite.go**：
- `drones` 表新增 `operator_id TEXT` 列（建表、迁移、查询 SELECT、INSERT、UPSERT）
- `GetActiveDrones` 查询和 Scan 中加入 `operator_id` 字段
- `SaveDrone` INSERT 和 UPDATE 中加入 `operator_id`

**frontend/src/App.vue**：
- 侧边栏详情面板中 Type/Standard 区域新增 "Op. ID" 显示行
- Operator 区域新增 `operator_id` 显示（条件渲染）

**frontend/src/views/DroneDetail.vue**：
- Operator 区域新增 "Op. ID" 显示行
- 面板可见性条件扩展为 `drone?.operator_latitude || drone?.operator_id`

**frontend/src/views/Drones.vue**：
- 表格新增 "Op. ID" 列（位于 ID 和 MAC 之间）

编译验证通过。

## 修复操作者 ID 被当作 UAS ID 的问题

参考 `/Users/luolitao/remoteid/RemoteID/open-remote-id-parser-master/include` 中的类型定义，将 Operator ID 与 UAS ID 分离存储和显示。

修改文件：

**backend/pkg/types/models.go**：
- `DroneData` 新增 `OperatorID string` 字段（json: `operator_id`）

**backend/internal/drone/processor.go**：
- `updateDroneFromMessage` 新增 `"operator_id"` case，将消息中的 `operator_id` 存入 `drone.OperatorID`

**backend/internal/db/sqlite.go**：
- `drones` 表新增 `operator_id TEXT` 列（建表、迁移、查询 SELECT、INSERT、UPSERT）
- `GetActiveDrones` 查询和 Scan 中加入 `operator_id` 字段
- `SaveDrone` INSERT 和 UPDATE 中加入 `operator_id`

**frontend/src/App.vue**：
- 侧边栏详情面板中 Type/Standard 区域新增 "Op. ID" 显示行
- Operator 区域新增 `operator_id` 显示（条件渲染 `v-if="selectedDrone.operator_id || selectedDrone.operator_latitude"`）

**frontend/src/views/DroneDetail.vue**：
- Operator 区域新增 "Op. ID" 显示行
- 面板可见性条件扩展为 `drone?.operator_latitude || drone?.operator_id`

**frontend/src/views/Drones.vue**：
- 表格新增 "Op. ID" 列（位于 ID 和 MAC 之间）
- colspan 从 8 更新为 9

编译验证通过。
